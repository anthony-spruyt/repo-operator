# Path-specific trivy ignores
# https://trivy.dev/latest/docs/configuration/filtering

vulnerabilities: null
secrets: null
misconfigurations:
  # Devcontainer image is driven by VS Code / envbuilder lifecycle,
  # not a long-running service — HEALTHCHECK is not applicable.
  - id: AVD-DS-0026
    paths:
      - src/templates/.devcontainer/Dockerfile
